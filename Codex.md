# Draft Repo Memory

This repository follows the secure Zoolanding draft release workflow.

- Run `git pull --ff-only` before work when the worktree is clean.
- For multi-repo work, also pull the hub repo and every affected `draft-*` repo when clean.
- If the worktree is dirty, report it before pulling or changing files.
- Work on `dev`, promote with PR `dev -> test`, then PR `test -> main`.
- `dev` does not deploy.
- `test` deploys the test draft only after merge to `test`.
- `main` deploys production only after merge to `main`.
- Native GitHub branch protection should protect `test` and `main` when the account plan supports it. If GitHub blocks protection for private repos, the deploy workflow still rejects push-triggered deploys unless the commit is a merge from the expected source branch, but GitHub cannot block the push itself.
- Treat this repository as public unless verified otherwise. Before making it public, before PR, and before merge, run the hub repo public-safety audit and resolve every blocking finding.
- Do not commit secrets, tokens, API keys, signed URLs, `.env*`, local logs, PDFs/CVs, private keys, certificates, local databases, credential JSON, local agent state, `ai_notes/`, `findings/`, or `errors-reports/`.
- Public contact details in draft content are allowed only when they are intentionally client-facing; personal source files, CVs, private photos, identity documents, and raw research stay local-only.
- Deployment uses GitHub OIDC to assume AWS IAM roles split by repo and environment; do not add long-lived AWS access keys.
- 2026-06-09 01:36 CT: Optional auth pilot is plan-only for now. `site-config.json` may expose only public `runtime.authRemote` with `authProfileId: "staff"` and `/auth/runtime-config`; `server/auth-profile-registry.json` keeps Cognito policy server-only with `status: "planned"` and non-secret social IdP references. No real Cognito, IdP credentials, JWT tokens, client secrets, deploy, or AWS calls were created for this pilot.
- 2026-06-09 03:39 CT: `tools/deploy-draft.mjs` must keep `server/auth-profile-registry.json` classified as `server-auth-profile-registry` and `server/integrations.json` as `server-integrations`, with no `pageId`, so GitHub Actions publishes auth/integration policy only through server-only authoring kinds.
- 2026-06-10 01:45 CT: For the Zoosite `staff` auth pilot, Google and Facebook are optional per draft/auth profile and are disabled by omission. `server/auth-profile-registry.json` currently has no `socialIdpSecretRefs`, so Cognito provisioning should not require `/zoolanding/auth/zoosite/staff/google` or `/facebook` secrets. If social login is needed later, add only non-secret secret references here and load the actual IdP client secrets manually into AWS Secrets Manager outside the repo.
- 2026-06-10 02:10 CT: Zoosite `staff` auth is now active with Cognito-native login only. Runtime auth resolves to user pool `us-east-1_Pq5OCadbK`, public app client `16jb6ml9q5jdh6blj7f668fajp`, Hosted UI `https://zoosite-staff-planned.auth.us-east-1.amazoncognito.com`, and groups `zoosite-client` / `zoosite-admin`. Google/Facebook remain disabled by omission for this draft/profile; re-enable only by adding non-secret refs here and loading real IdP secrets outside the repo.
- 2026-06-17 02:18 CT: Zoosite `staff` auth uses `environmentClaim: "custom:zoolanding_env"` with `customAuth.signup.setEnvironmentClaim: true`. The browser must not send this value; the API proxy stack assigns `test` or `prod` server-side. Console-created users need this mutable Cognito custom attribute set before passing environment-scoped auth.
- 2026-06-17 19:53 CT: Zoosite custom auth buttons should bind `config.loading` from `authForm.<action>.state === "loading"` and provide Spanish `loadingLabel` copy so sign-in, registration, confirmation, password recovery/reset, and logout visibly block repeat submits while serverless auth requests are pending. This remains UI feedback only; account authorization stays in the auth-admin BFF and Cognito/server-side policy.
- 2026-06-18 19:31 CT: Zoosite auth pages should pair button loading labels with visible spinner/status blocks for slow operations, including sign-in, registration, confirmation/resend, password recovery/reset, MFA challenge/setup, voluntary MFA enrollment, MFA disablement, and admin mutations. Keep this implemented through generic components and draft config so future auth drafts can reuse or restyle the same pattern.
- 2026-06-18 20:16 CT: Zoosite `/admin/usuarios` v1 UX should stay generic/config-driven. Explain pending/client/admin/suspended states in page config, derive card help/disabled admin actions from mapped `auth-admin` data-source fields such as `approvalStatus`, and keep all mutations behind BFF same-origin session, CSRF, tenant, and environment checks. Do not expose tokens, raw Cognito sessions, client secrets, or real user data in draft config.
- 2026-06-18 03:42 CT: Zoosite `staff` TOTP MFA is optional and self-service from `/mi-cuenta`. The server-only profile sets the authenticator issuer to `zoositioweb`, account label to the user's email, and friendly device name to `zoositioweb acceso`. Disabling MFA from the account page must use the generic `authFormAction:disableMfa` flow with active BFF session, CSRF, current password, and current authenticator code; lost-device recovery remains an administrator/support operation.
- 2026-06-18 12:51 CT: Zoosite `/mi-cuenta` should branch MFA UI from mapped public MFA metadata returned by the auth-admin BFF from Cognito `AdminGetUser`. Show the enrollment form only when software-token MFA is false, show the disable form only when true, and show a fallback message if the BFF returns `mfa.status: "unknown"`.
- 2026-06-18 13:19 CT: The auth-account runtime data source maps only declared `fields`, so Zoosite MFA conditions must use mapped fields such as `remote.auth.account.items.0.mfaSoftwareTokenEnabled` and `remote.auth.account.items.0.mfaStatus`; conditions must not depend on the raw nested `account.mfa` object unless the mapper is changed to preserve it.
- 2026-06-18 17:43 CT: Zoosite `/verificar-acceso` must map generic `authForm.respondMfaChallenge.errorCode` values to Spanish copy. `auth_environment_mismatch` means the user belongs to another Cognito environment such as production while using testing; `auth_challenge_expired` means the 5-minute challenge expired and the user must restart sign-in. Do not show either case as a generic wrong-code message.
- 2026-06-18 18:01 CT: Zoosite `staff` auth opts in to `environmentClaimMode: "list"` in the server-only profile so a verified Cognito `custom:zoolanding_env` value like `prod,test` can pass both testing and production. Keep this as an explicit operational grant for selected accounts; normal self-registration still writes the current stack environment.
- 2026-06-22 22:29 CT: Zoosite content hub admin reads use the shared BFF path `/features/content-hub` through `runtime.contentHubs[].publicApiBasePath`. Do not restore the stale `/content-hub` value; the front door only routes exact `/features/content-hub/read` and `/features/content-hub/action` paths to the content-hub Lambda.
- 2026-06-23 CT: Zoosite blog MVP draft pages render public discovery from the safe `runtime.contentHubs.publicArticles/publicTaxonomy` index and protected admin reads/actions through `/features/content-hub`. Creation action results expose returned `articleId`/`revisionId` from `remoteStatus.contentHub.content_hub_create_article`; draft JSON still cannot compose post-success admin URLs from those stored variables, so open editor/preview/SEO/versiones from the article table, which appends `articleId` as a query param.
- 2026-06-23 00:46 CT: Zoosite blog admin mutation buttons depend on generic `interaction-scope` snapshots plus table `rowData`; keep form fields inside their scope and keep table actions limited to explicit `eventPayloadFields`. Media upload actions use a single `upload` field per submission and display safe `assetId`/`fileName` response refs. Category/tag tables use `taxonomyId` as row ID and backend `label` as display text, while the authored form field can still be named `translation`.
- 2026-06-25 02:59 CT: Zoosite auth/account/blog/admin pages use the shared draft `siteHeader`/`siteFooter` and a shared hamburger modal menu. The `auth-account` runtime data source may run globally only for safe account navigation state mapped through `remote.auth.account` and `remoteStatus.authAccount`; admin-user lists and mutations must stay page-scoped and BFF-protected. The Cognito public app client keeps refresh tokens at 30 days but uses 12-hour access/id tokens so the server-cookie session can stay open longer without exposing tokens in the browser.
- 2026-06-26 18:40 CT, updated 20:02 CT: Zoosite blog editor pages use protected `content_hub_article_detail` to hydrate public-safe article metadata from `/admin/blog/articulos/:id/...` route param `id`. Keep editor/preview/SEO/versiones opened from the article table or with an explicit route ID; do not put package keys, signed URLs, server policy, or private storage fields in draft config.
- 2026-06-26 20:28 CT: Zoosite blog admin article action tables use `generic-table.actionButtonStyles` for 52px inline action links because the runtime may not have generated every dynamic Angora sizing class from draft JSON. Keep the tooltip-only labels and localized `Acciones` header; do not shrink these row actions below mobile-friendly touch targets.
- 2026-06-29 15:53 CT: Zoosite blog admin access now uses dedicated Cognito groups for product roles. The real pool `us-east-1_Pq5OCadbK` must include `zoosite-blog-editor`, `zoosite-blog-publisher`, `zoosite-blog-media`, `zoosite-blog-moderator`, and `zoosite-blog-analyst`; the server-only `staff` auth profile allowlist must match. `/admin/blog/moderacion` allows admin plus moderator, and `/admin/blog/analiticas` allows admin plus analyst.
- 2026-06-30 00:35 CT: Zoosite public article detail must render the published bundle body through the merged `articleContent` runtime variable. Do not bind the body paragraph back to `contentHub.currentArticle.summary`; summary is for metadata/intro, while the published package owns the actual article body.
- 2026-06-30 10:29 CT: Zoosite `/admin/blog` is an operational overview with row-scoped article links only. Do not restore global validate/publish buttons there; validation, approval, publishing, scheduling, archiving, and restore flows must happen from a selected row link or a detail page that carries a concrete article ID and revision ID when needed.
- 2026-07-01 20:26 CT: Zoosite `/admin/combos` must use the public form field `comboGroups` for combo group/theme metadata. Do not expose `groups` in `runtime.apiActions[].inputFields`; Angular maps `comboGroups` to backend `groups` after public runtime validation.
- 2026-07-01 21:23 CT: Zoosite `/admin/blog/articulos/:id/editor` now submits advanced visual structure with the article save flow. Keep `componentTreeJson` inside the editor interaction scope and `content_hub_update_package.inputFields`; Angular maps valid advanced JSON to content-hub `components` only when `advancedMode` is active, and malformed JSON must block before the backend call.
- 2026-07-01 22:08 CT: Zoosite `/admin/blog/articulos/:id/editor` previews the current advanced JSON through app-level `generic-component-preview`, not a static card. Keep preview JSON event-free and limited to allowed generic component types. SEO and scheduling buttons mirror content-hub article status for UX, but all editorial transitions remain enforced by content-hub server-side.
